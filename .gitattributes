# Git Attributes - настройки для автоматической обработки файлов в git
# Этот файл определяет, как git должен обрабатывать различные типы файлов

# Shell scripts - текстовые файлы с LF окончаниями строк
# Это гарантирует, что скрипты будут работать одинаково на всех системах
*.sh text eol=lf
